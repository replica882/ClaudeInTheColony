# 规划

## 这东西要成为什么

不是「AI 替你玩缺氧」。是**一个人和一个 AI 一起玩缺氧**。

人操作，AI 做人做不到或懒得做的那部分：

- **看见人看不见的** —— 温度全景、气压分布、全局统计。游戏里要切三次图层才看得到的东西
- **算人懒得算的** —— 产能平衡、热平衡、电力峰值、还能撑几个周期
- **记住人忘掉的** —— 这根管子当初为什么这么走
- **在关键时刻喊一声** —— 要炸了 / 要断氧了
- **干重复劳动** —— 大片挖掘标记、批量改优先级

## 分层

```
L0  游戏进程内（C# mod）
    只做两件事：把状态吐出来、把指令投递到主线程执行
    ⚠️ 改这一层要重启游戏 —— 会打断人玩

L1  数据接口（HTTP/JSON @ 127.0.0.1:7373）
    稳定契约

L2  认知层（Python）
    渲染、分析、知识、记忆
    改这一层不用重启，随便迭代

L3  MCP
L4  Claude
```

### 第一原则：L0 越薄越好

**任何能放到 L2 的逻辑，都不许放在 L0。**

因为 L0 每改一次就要打断人玩一次。渲染器放在外面就是这条原则的产物：
调配色、加图层、换画法，游戏一直开着不用管。

推论：**L0 要一次性补完，然后尽量冻结。**

## 能力清单

### 眼睛（看）

- [x] 殖民地 / 周期 / 复制体（名字、坐标、血量）
- [x] 格子数据：元素、温度、质量（`/raw`，二进制）
- [x] 建筑清单：类型、坐标、朝向、运行状态、功率
- [ ] **间歇泉 / 喷口**：类型、产量、喷发周期、休眠期、当前状态
- [ ] **资源存量**：水、氧、食物、矿石……
- [ ] 电力网络：每条电路的发电 / 耗电 / 电池存量
- [ ] 管道网络：液 / 气 / 固三套的拓扑
- [ ] 自动化逻辑网络
- [ ] 研究进度
- [ ] 星图 / 火箭（上太空之后就需要）
- [ ] 游戏当前的警报列表

### 嘴（说）

- [ ] 游戏内通知 —— 在人玩的时候直接跳出来说话

### 手（做）

原则：**只做等价于人自己点鼠标的事。**
下指令、标记、让复制体去执行，全部可撤销。
绝不碰沙盒接口（凭空改格子、改温度）——那不是玩游戏，是作弊，而且不可逆。

- [ ] 暂停 / 加速
- [ ] 标记挖掘 / 取消标记
- [ ] 改建筑优先级
- [ ] 开关设备
- [ ] 建造蓝图（要选材料、定朝向，容易搞砸，放最后）

### 脑（算）← 价值最高，而且全在 L2

- [ ] 氧气平衡：产 vs 耗
- [ ] **水平衡：存量、净变化、还能撑几周期、缺口多大**
- [ ] 电力平衡：峰值 vs 容量
- [ ] 食物平衡：卡路里
- [ ] 热平衡（缺氧最难的一块）
- [ ] 模块识别：把一坨建筑认出来是「一个 SPOM」

### 记忆（长期）

- [ ] 定期快照存成时间序列
- [ ] 趋势：水在掉，按这个速度还有几个周期

## 当前的判断

一直在做「眼睛」，接着想做「手」。但**价值最高的是「脑」** ——
把看到的数据变成判断。而且「脑」整个跑在 L2，**不用重启游戏**。

> 例：「有点缺水，低温喷口喷很慢」——
> 这不需要手，需要的是：现在有多少水、每周期净变化多少、
> 喷口的喷发/休眠周期是什么、平均产量多少、缺口多大。
> 全是「脑」和「记忆」的活。

所以顺序是：

1. **把 L0 一次补完**（趁游戏还没重启）：手 + 嘴 + 剩下的眼睛
   （间歇泉、资源存量、电力、管道、警报）
2. 重启一次
3. **之后所有力气放在 L2**，游戏一直开着

## 设计决策记录

| 决策 | 为什么 |
|---|---|
| 渲染放游戏外面 | 改配色不用重启。L0 越薄越好 |
| 不用 Rust | 瓶颈是「画法调得对不对」，不是性能。Python 改完立刻跑 |
| 自己解析 HTTP，不用 HttpListener | Mono 各平台行为不一致，而只需要 GET/POST + JSON |
| 手写 JSON 序列化 | 绕开游戏自带 Newtonsoft 的版本冲突 |
| 只做等价点鼠标的操作 | 可撤销。沙盒接口不可逆，玩坏了没法还原 |
| 图 + ASCII 两种输出 | 大处看结构用图，小处数格子用字符，后者还省 token |
| 请求走主线程队列 | Unity 单线程，后台线程碰游戏对象会随机崩溃 |
